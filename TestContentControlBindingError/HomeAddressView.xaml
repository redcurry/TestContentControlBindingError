<UserControl x:Class="TestContentControlBindingError.HomeAddressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestContentControlBindingError"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical">
        <TextBox Text="{Binding Street}" Width="100" />
        <TextBox Text="{Binding City}" Width="100" />
        <TextBox Text="{Binding State}" Width="100" />
        <TextBox Text="{Binding ZipCode}" Width="100" />
    </StackPanel>
</UserControl>
